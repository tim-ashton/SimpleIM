# SimpleIM


## Building

cmake --preset linux
cmake --build --preset linux

## Running

There is a launch config for VS Code included in the git repository.

Use the built in launch, selecting the desired setup.